# Processed Data

This folder contains preprocessed datasets ready for training and evaluation.

## Data Groups

- `regression/`: preprocessing outputs for the regression task.
- `classification/`: preprocessing outputs for the classification task.

## Reproducibility Notes

- Every file in `processed/` must be traceable back to data in `raw/`.
- If preprocessing changes, update the related notebook/script and README accordingly.
